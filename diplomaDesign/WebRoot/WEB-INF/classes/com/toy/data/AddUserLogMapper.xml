<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.toy.data.AddUserLogMapper">
	<insert id="insertUserLog" parameterType="map">
		INSERT INTO user_log(log_id, log_name, log_password, log_emp_id)
		VALUE(NULL, #{logName}, #{passWord}, #{empId});
	</insert>
</mapper>
